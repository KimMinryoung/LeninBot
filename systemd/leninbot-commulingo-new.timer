[Unit]
Description=Add five new CommuLingo people daily (DeepSeek off-peak window)

[Timer]
# One item per fire; 5 fires inside the DeepSeek off-peak discount window
# (UTC 16:30-00:30). No Persistent: a missed fire must not run at peak price.
OnCalendar=*-*-* 17..21:00:00

[Install]
WantedBy=timers.target
